// const list = document.querySelector("#categories");
// const listsElements = document.querySelectorAll("li.item");

// console.log(`${listsElements.length}`);

// listsElements.forEach((listElement) => {

//   const categories = listElement.querySelector("h2").textContent;
//   const sublist = listElement.querySelectorAll("ul li");

//   console.log(`${categories}`);
//   console.log(`${sublist.length}`);
// });
